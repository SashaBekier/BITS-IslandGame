using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "Item_PuzzlePiece", menuName = "ScriptableObject/Item - Puzzle Piece")]
public class Item_PuzzlePiece : Item
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
